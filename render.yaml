services:
  # Backend Service (Rust/Axum)
  - type: web
    name: near-treasury-backend
    runtime: rust
    plan: starter
    rootDir: nt-be
    buildCommand: cargo build --release --bin nf-be
    startCommand: ./target/release/nf-be
    envVars:
      - key: PIKESPEAK_KEY
        sync: false
      - key: FASTNEAR_API_KEY
        sync: false
      - key: SPUTNIK_DAO_API_BASE
        sync: false
      - key: RUST_LOG
        value: info
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: database
          property: connectionString
    healthCheckPath: /api/health

  # Frontend Service (Next.js)
  - type: web
    name: near-treasury-frontend
    runtime: node
    plan: starter
    rootDir: nt-fe
    buildCommand: bun install && bun run build && cp -r public .next/standalone/ && cp -r .next/static .next/standalone/.next/
    startCommand: node .next/standalone/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_BACKEND_API_BASE
        fromService:
          type: web
          name: near-treasury-backend
          envVarKey: RENDER_EXTERNAL_URL
    healthCheckPath: /

databases:
  - name: database
    plan: free
